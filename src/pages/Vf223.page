<apex:page controller="Example223">
    <apex:form >
        <apex:pageBlock >
        <apex:pageBlockTable value="{!opportunities}" var="a">
            <apex:column value="{!a.name}"/>
                <apex:column value="{!a.amount}"/>
                <apex:column value="{!a.stageName}"/>
            </apex:pageBlockTable>
            </apex:pageBlock>
    </apex:form>
</apex:page>